# Security Model

- Developers do not access production servers
- Jenkins uses a limited deploy user
- SSH key-based authentication only
- No passwords in pipelines
- Secrets excluded from version control
